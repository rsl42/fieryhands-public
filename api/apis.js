const APIS = Object.freeze({
    client: {
        announcement: 'announcement',
    },
    admin: {
        announcement: 'admin/announcement',
    },
    shared: {
        file: 'file'
    }
})

export default APIS;